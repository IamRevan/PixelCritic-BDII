/** @type {import('next').NextConfig} */
const nextConfig = {
  // Permitir que Next.js sirva archivos estaticos de la carpeta public
  // (los HTML originales se mantienen como referencia de diseno)
  reactStrictMode: true,
};

module.exports = nextConfig;
